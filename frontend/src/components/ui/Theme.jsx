import { createTheme } from '@mui/material';

const Theme = createTheme({
    palette: {
        primary: {
            main: '#1e1e2f',
        },
        secondary: {
            main: '#e0d8c3',
        },
    },
});

export default Theme;